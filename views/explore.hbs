

<!--<div class="posSearch">
<form  action="/explore/search">
<button class="searchBarBtn" type="submit">SEARCH</button>
  <input class="searchBar" id='searchBar' name="search" type="text" placeholder="Search"
    aria-label="Search">
</form>
</div>

<body>
<div>
{{#each posts}} 
<div class="nameOfArt"> {{this.title}} </div>
<div class="exploreimg"> <img src={{this.img}} alt=""></div>
 <div class="postsInfo">
 <div class="nameOfArt">{{this.title}}</div>
 <div class="infoExplore">"{{this.description}}". The artist is {{this.artist}}. You can find the work in {{this.area}}, {{this.adress}}.</div> 
  </div>
{{/each}}
</body>-->
<head>
  <div class="posSearch">
    <form action="/explore/search">
      <!--<button class="searchBarBtn" type="submit">SEARCH</button>-->
      <input class="searchBar" id='searchBar' name="search" type="text" placeholder="Search"
      aria-label="Search">
    </form>
  </div>
</head>
<body>
  <div>
    {{#each posts}}
    <div class="nameOfArt"> {{this.title}} </div>
    <div class="exploreimg"> <img src={{this.img}} alt=""></div>
    <div class="postsInfo">
      <div class="bb">{{this.title}}</div>
      <div class="infoExplore">"{{this.description}}". The artist is {{this.artist}}. You can find the work in
        {{this.area}}, {{this.adress}}.</div>
    </div>
    {{/each}}
</body>

